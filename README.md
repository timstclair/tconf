Tim's configuration
===================

This is my repository of configurations which I use across machines.


Initialize submodules
---------------------
This repository makes use of git submodules. To recursively fetch all of them, use:

$ git submodule update --init --recursive
