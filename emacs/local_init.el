;;
;; Local initialization template
;;
;; This file is a starting point for a local init.el to be copied. It should not
;; be modified in place, but rather should be copied to .emacs.d/init.el and
;; modified there.

;; Location of tconf init.el
(load-file "~/tconf/emacs/init.el")

;; Location to store autosaves in
(set-autosave-dir "~/.emacs.d/autosaves/")

;; Location to store backups in
(set-backup-dir "~/.emacs.d/backups/")
